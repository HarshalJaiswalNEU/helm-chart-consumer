apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todo-app-consumer.fullname" . }}-test-connection"
  labels:
    {{- include "todo-app-consumer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todo-app-consumer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
